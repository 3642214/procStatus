# procStatus
使用方法：
    ./pstat.sh "process name"

列
time,PID,CPU%,MEM%,VSZ,RSS,rchar,wchar,rbytes,wbytes,netIN,netOUT,used,buffers,cached
时间，进程号，cpu占用率，内存占用率，虚拟内存，物理内存，读，写，实际读磁盘，实际写磁盘，网络in，网络out，内存used，buffers，cached
